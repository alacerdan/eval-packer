{
  "name": "eval-packer",
  "version": "0.0.0",
  "description": "Packer",
  "main": "src/index.js",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release:patch": "git checkout main --quiet && npm version patch && git push && git push --tags",
    "release:minor": "git checkout main --quiet && npm version minor && git push && git push --tags",
    "release:major": "git checkout main --quiet && npm version major && git push && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/alacerdan/eval-packer.git"
  },
  "author": "Alã Damasceno",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/alacerdan/eval-packer/issues"
  },
  "homepage": "https://github.com/alacerdan/eval-packer#readme"
}
